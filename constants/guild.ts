//

export const INITIAL_COLOR = "#535a66";
export const FILE_NAME_RULE = "Use '-' to separate words in file names";
export const ALLOWED_TYPES = ["outline", "solid"];
export const ALLOWED_MERGE = `Allow up to ${ALLOWED_TYPES.length} icon types`;
